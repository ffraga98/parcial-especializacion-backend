server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ffraga98/parcial-especializacion-backend
          default-label: main
          force-pull: true
          search-paths: configuration